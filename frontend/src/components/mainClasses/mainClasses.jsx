import './mainClasses.scss'

const MainClasses = ({title, list, image}) => {
    return (
        <section id='mainClasses'>
            <div></div>
        </section>
    );
};

export default MainClasses;